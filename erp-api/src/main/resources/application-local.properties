# Change your URL to this exactly. Note the 'options' part.
spring.datasource.url=jdbc:postgresql://localhost:5432/erp_db?options=-c%20timezone=UTC
spring.datasource.username=admin
spring.datasource.password=password

# Add this to force Hibernate to use a specific timezone
spring.jpa.properties.hibernate.jdbc.time_zone=UTC