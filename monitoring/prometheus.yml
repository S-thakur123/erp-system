global:
  scrape_interval: 15s # How often to fetch metrics
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'erp-system-api'
    metrics_path: '/actuator/prometheus' # The endpoint we enabled in Spring Boot
    static_configs:
      - targets: ['host.docker.internal:8080'] # Points to your Java app